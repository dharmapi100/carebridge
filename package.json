{
  "name": "carebridge-compliance-engine",
  "version": "1.0.0",
  "description": "CareBridge Compliance Engine MVP",
  "main": "src/api/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/api/server.js",
    "test": "node src/services/testRunner.js && for f in src/tests/test_*.js; do echo \"=== $f ===\"; node \"$f\" || exit 1; done"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^4.19.2"
  }
}
